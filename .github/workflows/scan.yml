on: [push]

jobs:
  zap_scan:
    runs-on: ubuntu-latest
    name: Scan the web app
    steps:
      - name: ZAP Scan
        uses: zaproxy/action-baseline@v0.7.0
        with:
          target: 'https://cloud.zambion.com'
